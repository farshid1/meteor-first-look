template(name="appAtInput")
    .form-group.has-feedback
        input.form-control(type=options.type, placeholder=options.placeholder)
        span.glyphicon.form-control-feedback(class=options.icon)